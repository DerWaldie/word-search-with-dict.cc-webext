{
  "name": "dict.cc-webext",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Julius Rummel <32172513+DerWaldie@users.noreply.github.com>",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p build && ./node_modules/.bin/web-ext build --overwrite-dest -s ext/ -a build/",
    "clean": "rm -r build/",
    "lint": "./node_modules/.bin/web-ext lint -s ext/"
  },
  "devDependencies": {
    "web-ext": "^3.0.0"
  }
}